# 全项目规范审查报告（2026-06-14 第二轮）

> **审查范围**：全部 16 个业务模块，前端 + 后端全覆盖。
> **审查依据**：`docs/standards/conventions.md` + `docs/standards/review-scope.md`。
> **状态**：**✅ 全部修复完成（2026-06-15）**。原发现第二档违规 15 处（F3:12 + F13:2 + F10:1），已逐项修复。无新增第一档问题。

---

## 一、工具检查结果

| 检查项 | 命令 | 结果 |
|--------|------|------|
| TypeScript 类型检查 | `tsc --noEmit` | ✅ 零错误 |
| ESLint | `eslint . --max-warnings=0` | ✅ 零警告 |
| Rust 编译检查 | `cargo check` | ✅ 通过 |

---

## 二、已确认合规项（重新验证）

| 检查项 | 约定 | 本轮结果 |
|--------|------|----------|
| queryKey 工厂化 | F4 | ✅ 全部走 `workspaceQueryKeys` |
| mutation onSuccess invalidateQueries | F4 | ✅ 全部含 invalidate |
| mutation onError toast | F4 | ✅ 全部含 `toast.error` |
| `motion.div` 零使用 | F2 | ✅ 全部使用 `m` 别名 |
| i18n 内联默认文案 | F5 | ✅ 零处 `t("key","中文")` |
| z-index 走 `UI_LAYERS`（组件 className） | F10 | ✅ 全部走常量 |
| `cn()` 类名拼接 | F7 | ✅ 未发现手写拼接 |
| `useReducedMotion` | F2 | ✅ 动画组件已覆盖 |
| command 路径 unwrap 零使用 | B2 | ✅ 零处 panic |
| `write_tx` 事务 | B1 | ✅ 多步写均包裹 |
| 数据库迁移在事务内 | B3 | ✅ |
| Capabilities 最小权限 | B5 | ✅ |
| CSS 变量 + Tailwind 语义类 | F7 | ✅ |
| `json.parse(json.stringify())` 零使用 | F11 | ✅ |
| `generateId()` 统一使用 | F11 | ✅ |
| `@ts-ignore` / `@ts-expect-error` / `as any` | F12 | ✅ src/ 零使用（bindings.ts 自动生成除外） |
| 服务端数据进 zustand | F4 | ✅ 零违规 |
| ErrorBoundary 页面级覆盖 | F8 | ✅ app-shell.tsx 已包 |
| 弹窗删除按钮 variant destructive | F9 | ✅ 已修复 |
| `window.confirm` 原生弹窗（非 shopping-delete.ts） | F9 | ✅ 已修复 5 处 |

---

## 三、存量技术债对照（不重复报）

以下两项已在 `conventions.md` 和上轮 `bug.md` 中跟踪，本轮不再作为新缺陷报告：

| ID | 简述 | 跟踪位置 |
|----|------|----------|
| B-BUG-02 | 全后端 `Result<T, String>` 无 `thiserror`（165+ 处） | `conventions.md` B2 |
| B-BUG-04 | 13 个 JSON 模块无 Rust 侧输入校验 | `conventions.md` B4 |
| 弹窗改造清单 | 删除确认/回车提交/自动聚焦/脏数据确认等 | `conventions.md` 弹窗改造清单 |
| shopping-delete `window.confirm` | 有撤销安全网，豁免 | `conventions.md` 弹窗改造清单 #1 |

---

## 四、新增违规发现

### 🟡 第二档 — F3：表单手写校验未使用 zod（12 处，6 个模块）— ✅ 全部已修复

> **判级依据**：F3 硬约定"现有的散落手写校验…一律视为待迁移项——审查时照报"。这些文件在 `handleSubmit` 中逐字段 `if/return`，未使用 zod schema + zodResolver。
>
> **修复方式**：全部迁移到 `useForm` + `zodResolver` + zod v4 schema，同步补 F9 保存按钮 `disabled={isPending || !canSubmit}` + pending 文本。

| # | 文件 | 行号 | 状态 | 修复方式 |
|---|------|------|------|----------|
| 1 | `reflection-page.tsx` | 552-554 | ✅ | `reflectionFormSchema` + `Controller` 替换 `useState` |
| 2 | `finance-entry-edit-dialog.tsx` | 108-116 | ✅ | `entryFormSchema` + `Controller` 替换 `useState` |
| 3 | `emotion-edit-dialog.tsx` (Entity) | 497-500 | ✅ | `z.record(z.string(), z.string()).superRefine(...)` + `useForm` + `form.watch()` 兼容 EntityFields |
| 4 | `emotion-edit-dialog.tsx` (Overview) | ~915 | ✅ | `emotionOverviewFormSchema` + `Controller` 替换 `useState` |
| 5 | `future-edit-dialog.tsx` (Blueprint) | 96-98 | ✅ | `blueprintFormSchema` + `Controller` 替换 `useState` |
| 6 | `future-edit-dialog.tsx` (Milestone) | ~188 | ✅ | `milestoneFormSchema` + `Controller` 替换 `useState` |
| 7 | `future-edit-dialog.tsx` (Experiment) | ~324 | ✅ | `experimentFormSchema` + `Controller` 替换 `useState` |
| 8 | `journey-edit-dialog.tsx` (Memory) | 454-461 | ✅ | `memoryFormSchema` + `useForm` + `form.watch()` 兼容现有字段组件 |
| 9 | `journey-edit-dialog.tsx` (Growth Node) | ~647 | ✅ | `growthFormSchema.superRefine(...)` + `useForm` + `form.watch()` |
| 10 | `journey-edit-dialog.tsx` (Anchor) | ~830 | ✅ | `anchorFormSchema` + `useForm` + `form.watch()` |
| 11 | `journey-edit-dialog.tsx` (Thread/Line) | ~943 | ✅ | `textEditFormSchema` + `Controller` 替换 `useState` |
| 12 | `socioeconomics-edit-dialog.tsx` | 659-661 | ✅ | `promptFormSchema` + `Controller` 替换 `useState` |

---

### 🟡 第二档 — F13：组件文件混入非组件导出（2 处）— ✅ 全部已修复

> **判级依据**：F13 硬约定"包含用户可见组件的文件只导出组件（含 export type）——不混入 export const 常量或 export function 工具函数"。
>
> **修复方式**：hook 提取到独立文件，原组件文件只保留组件导出。

| # | 文件 | 行号 | 状态 | 修复 |
|---|------|------|------|------|
| 1 | `src/components/ui/liquid-glass.tsx` | 63 | ✅ | `useLiquidGlassPointer` → `liquid-glass-hook.ts`，更新 `tabs.tsx` 引用 |
| 2 | `src/components/ui/popup-notification.tsx` | 313 | ✅ | `usePopupNotification` + `PopupNotificationContext` → `popup-notification-hook.ts`，更新 `rhythm-popup.tsx` 引用 |

---

### 🟡 第二档 — F10：硬编码 z-index（1 处）— ✅ 已修复

> **判级依据**：F10 硬约定"浮层 z-index 只用 `UI_LAYERS` 语义层级，禁止在组件里硬编码 z-[999] 之类魔法数字"。

| # | 文件 | 行号 | 状态 | 修复 |
|---|------|------|------|------|
| 1 | `src/features/bettertolive/ui/worldhistory/star-map-canvas.tsx` | 97 | ✅ | `ui-layers.ts` 新增 `starMapLink: "z-[1]"`，组件引用 `UI_LAYERS.starMapLink` |

---

## 五、已确认无需处理项

以下在审查中被标记但经核实不构成违规：

| 项 | 文件 | 原因 |
|----|------|------|
| 图谱 `FALLBACK_THEME_TOKENS` 硬编码色值 | `cytoscape-2d-graph.tsx`、`react-force-graph-3d-graph.tsx` | 符合 F7.1「SSR 安全网」模式 — fallback 默认值在 `document` 不可用时使用，与 `readToken(name, fallback)` 一致 |
| Three.js 3D 光照 hex 值 | `react-force-graph-3d-graph.tsx:849,863,981,983,989` | Three.js 渲染引擎参数，非 CSS 变量体系范围，属第三档视觉细节 |
| `export type` 与组件共存 | `multi-select.tsx`、`transfer-list.tsx`、`emotion-edit-dialog.tsx` 等 9 个文件 | F13 明确允许 `export type` |
| `shopping-delete.ts` 导出函数/常量 | `shopping/_shared/shopping-delete.ts` | 非 `.tsx` 组件文件，不适用 F13 |
| `bindings.ts` 中 `as any` | `src/bindings.ts:561` | 自动生成文件，约定 F13 明确"不得手写/修改 bindings.ts" |

---

## 六、汇总表

| ID | 维度 | 严重程度 | 简述 | 文件 | 状态 |
|----|------|----------|------|------|------|
| F-AUDIT-01 | 前端 | 🟡 | reflection-page 手写校验 → zod | `reflection-page.tsx` | ✅ |
| F-AUDIT-02 | 前端 | 🟡 | finance-entry-edit-dialog 手写校验 → zod | `finance-entry-edit-dialog.tsx` | ✅ |
| F-AUDIT-03 | 前端 | 🟡 | emotion-edit-dialog 手写校验 (2处) → zod | `emotion-edit-dialog.tsx` | ✅ |
| F-AUDIT-04 | 前端 | 🟡 | future-edit-dialog 手写校验 (3处) → zod | `future-edit-dialog.tsx` | ✅ |
| F-AUDIT-05 | 前端 | 🟡 | journey-edit-dialog 手写校验 (4处) → zod | `journey-edit-dialog.tsx` | ✅ |
| F-AUDIT-06 | 前端 | 🟡 | socioeconomics-edit-dialog 手写校验 → zod | `socioeconomics-edit-dialog.tsx` | ✅ |
| F-AUDIT-07 | 前端 | 🟡 | liquid-glass.tsx 混出 hook → 分离 | `liquid-glass.tsx` | ✅ |
| F-AUDIT-08 | 前端 | 🟡 | popup-notification.tsx 混出 hook → 分离 | `popup-notification.tsx` | ✅ |
| F-AUDIT-09 | 前端 | 🟡 | star-map-canvas 硬编码 z-index=1 → UI_LAYERS | `star-map-canvas.tsx` | ✅ |

---

_报告生成：2026-06-14 | 修复完成：2026-06-15 | 15/15 ✅ | TypeScript 零错误 ✅ | ESLint --max-warnings=0 ✅ | Rust 编译通过 ✅_
