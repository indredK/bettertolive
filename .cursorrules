# bettertolive — AI Behavior Lock

&gt; ⚠️ 本文只是 Cursor 的物理入口。完整 AI 操作流程见项目根目录 `AGENTS.md`（唯一逻辑入口）。不要只读本文件就开始改代码。
&gt;
&gt; 这是 bettertolive 项目的**最高优先级规则文件**。所有 AI 工具（Claude Code, Cursor, Trae CN, Mimocode, CodeBuddy 等）在操作此项目时，必须以本文档作为行为的第一约束。
&gt;
&gt; **入口 vs 优先级（两件事，不矛盾）**：
&gt; - **入口顺序**：所有工具 → `AGENTS.md`（唯一逻辑入口，从哪开始读）。
&gt; - **裁决优先级**：内容冲突时 `.cursorrules &gt; AGENTS.md &gt; docs/*.md`（谁说了算）。
&gt; - `AGENTS.md` 是"门"，本文件 `.cursorrules` 是"最高法律"。

---

## §0 🛑 STOP 铁律（最高频引用，第一眼看到）

以下情况**必须停下问用户**，不许猜、不许自作主张：

1. **文档 / workflow 没覆盖你要做的事** → 停，问用户。
2. **你要做的事会违反 `docs/ARCHITECTURE.md` §2 任一条** → 停，问用户。
3. **验证链（`lint` / `typecheck` / `test` / `check:rust`）任何一条失败** → 停，不许带病提交、不许改测试让它过。
4. **涉及危险操作**（删数据 / 重置 DB / `git push` / `git add .`）→ 停，等用户明确同意。
5. **看不懂某个既有模式为什么这么写** → 先查 `docs/DECISIONS.md` 和 `docs/bugs/`，还不懂 → 问用户，不许重写。

---

## §1 真理源 (SSoT)

在执行任何代码操作前，**必须读取以下文件**（按此顺序，与 `AGENTS.md` 三步铁律的必读清单一致）：

1. `AGENTS.md` — 唯一逻辑入口，路由到工作流
2. `.cursorrules` (本文) — 行为约束（最高优先级）
3. `docs/ARCHITECTURE.md` §2 — 🔴 禁止模式（不可触犯）
4. `docs/standards/conventions.md` — 编码规范（强制/建议）
5. `docs/standards/review-scope.md` — 审查边界（什么算 bug、何时停）
6. `docs/AI-WORKFLOWS.md` — 工作流定义

**参考**（按需查阅）：`docs/development-workflow.md`（开发流程）、`docs/bugs/` 下最新审计报告（避免重复标记）。

---

## §2 🚫 绝对禁止

以下行为在任何情况下都不允许：

1. **不要修改 `docs/` 下文件而不更新对应模块的 roadmap/设计文档**
2. **不要跳过 `docs/ARCHITECTURE.md` §2 中的 14 条禁止模式**
3. **不要一次提交多个不相关的改动** — 每个 commit 聚焦一个逻辑变更
4. **不要使用 `git add .`** — 只 stage 与本次改动直接相关的文件
5. **不要推送 (`git push`) 除非用户明确要求**
6. **不要创建新 feature 模块而不注册到对应索引**（features → 对应模块目录，modules → `docs/modules/<id>/README.md`）
7. **不要在一个 `.tsx`/`.ts` 文件中混合视图、状态、业务编排和 IPC 调用**
8. **不要改了 Tauri command 不跑 `generate:bindings`** — specta 是唯一事实来源，前端 `bindings.ts` 自动生成，不得手写修改
9. **不要自行推断技术栈或库** — 以 `package.json` / `Cargo.toml` 和相邻文件 import 为准
10. **不要用 npm / pnpm / yarn** — 包管理器是 bun，仓库有 `bun.lock` 与 `.bunfig.toml`

---

## §3 工作流路由

当用户发出指令时，AI 应按以下规则路由：

| 用户指令关键词 | 路由到 |
|----------------|--------|
| review / 审查 / 审计 / 检查代码 | `docs/AI-WORKFLOWS.md → /review` |
| fix / 修复 / 改 bug / 修 bug | `docs/AI-WORKFLOWS.md → /fix` |
| doc / 文档 / 更新文档 / 对齐 | `docs/AI-WORKFLOWS.md → /doc` |
| feature / 新功能 / 开发 / 实现 | `docs/AI-WORKFLOWS.md → /feature` |

如果无法判断，询问用户。

---

## §4 强制验证链

所有代码改动完成后，必须执行以下验证（按顺序，一条失败即停止）：

```bash
# 1. ESLint 零容忍
bun run lint

# 2. TypeScript 类型检查
bun run typecheck

# 3. 单元测试
bun run test

# 4. Rust 后端检查（如果有 Rust 改动）
bun run check:rust
```

---

## §5 文档回写规则

每次改动合入后：

- 对应 `docs/modules/<id>/` 下的设计文档 / roadmap 须同步更新
- 如果修复了已知 bug，更新 `docs/bugs/` 下对应审计报告
- 如果发现新的架构约束，沉淀到 `docs/standards/conventions.md` 或 `docs/ARCHITECTURE.md`
- 不留下过时的 roadmap checkbox

---

## §6 输出格式约定

审计/审查报告统一格式：

```
[§X] `文件路径:行号` — 问题描述 — 修改建议 — **强制/建议**
```

示例：
```
[§2.2] `src/features/bettertolive/shopping/page.tsx:42` — 硬编码中文 "确认删除" — 移至 i18n locales — **强制**
```

---

## §7 项目元信息

| 属性 | 值 |
|------|-----|
| 项目名 | bettertolive |
| 包管理器 | bun（禁止 npm/pnpm/yarn） |
| 前端测试 | `bun run test` (vitest + happy-dom) |
| Rust 检查 | `bun run check:rust` (fmt + clippy + check) |
| 代码风格 | `bun run format` (prettier) |
| IPC 契约 | specta 自动生成，Rust 是唯一事实来源 |
| 工作流入口 | `AGENTS.md` |
